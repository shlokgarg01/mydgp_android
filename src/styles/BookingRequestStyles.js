import { StyleSheet } from "react-native";
import Colors from "../helpers/Colors";

const BookingRequestStyles = StyleSheet.create({
  requestCardContainer: {
    marginTop: 15,
    marginHorizontal: 10,
    elevation: 10,
    borderRadius: 10,
    backgroundColor: Colors.WHITE,
    padding: 10,
    paddingTop: 15,
  }
})

export default BookingRequestStyles